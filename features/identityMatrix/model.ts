export interface IdentityMatrix {
  id: string;
  userId: string;
  name: string;
  description: string;
  // bannerKey: string;
  // categories: Category[];
  createdAt: string;
  updatedAt: string;
}
